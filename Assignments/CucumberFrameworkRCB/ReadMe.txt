Pre-requesites:Maven,jdk 15 and above, Cucumber plugin hould be installed.
Steps:
   Pass desired input filepath into "FilePath" variable of src\test\resources\properties\config.properties file.
   Open command prompt pointing root folder having to the pom.xml ->  run the test by passing "mvn test" command.
   Check the test report in the report folder on the root.
